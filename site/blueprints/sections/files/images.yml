#
# @info File section for image files blueprints
# @path site/blueprints/sections/files/image.yml
# @date 20220707
#

label: 
  de: Bilder, Fotos, Grafiken
  en: Image files

image: false
layout: cards
limit: 25
parent: page.children.find('media')
size: medium
sortable: false
template: image
translate: false
type: files
uploads:
  template: image

empty: Keine Bild-Dateien
info: "{{ file.dimensions }}, {{ file.nicesize }}"
text: "{{ file.name }}"
